<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit Book</title>
    <link rel="stylesheet" href="/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>

<header>
    <%- include('navbar'); %>
</header>

<div class="container mt-5">

    <h1 class="mb-4">Edit Book</h1>

    <form action="/admin/books/edit/<%= book.id %>" method="POST" class="shadow p-4 rounded bg-white">

        <label class="form-label">Title</label>
        <input class="form-control mb-3" name="title" value="<%= book.title %>" required>

        <label class="form-label">Genre</label>
        <input class="form-control mb-3" name="genre" value="<%= book.genre %>" required>

        <label class="form-label">Author</label>
        <input class="form-control mb-3" name="author" value="<%= book.author %>" required>

        <label class="form-label">Price</label>
        <input class="form-control mb-3" type="number" name="price" value="<%= book.price %>" required>

        <label class="form-label">Image URL</label>
        <input class="form-control mb-3" name="image_url" value="<%= book.image_url %>" required>

        <label class="form-label">Description</label>
        <textarea class="form-control mb-3" name="description"><%= book.description %></textarea>

        <button class="btn btn-primary">Save Changes</button>
        <a href="/admin/books" class="btn btn-secondary ms-2">Cancel</a>

    </form>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
